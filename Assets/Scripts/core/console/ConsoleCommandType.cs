namespace SomeAimGame.Console {
    /// <summary>
    /// Enum holding all console command types.
    /// </summary>
    public enum ConsoleCommandType {
        UTIL,
        GAMEMODE,
        TARGET,
        SOUND,
        WIDGETS,
        CONTROLS,
        KEYBIND,
        CROSSHAIR
    }
}